# Publish Cycle — 2026-09-04

**Cycle:** Friday run, **applied** slot. Drafted, committed to `master`, newsletter sent. Live-URL verification blocked (**21st consecutive cycle**); verified at the source via the GitHub API instead.

---

## Gating checks

- `ops/reports/sign-off.md` — present, contains "Autonomous operation is authorized." (Pass.)
- `ops/alerts/` — 10 files, **zero IMMEDIATE-level**. Eight carry the 2026-08-25 RETIRED banner; the two genuinely open files (`linkedin-post-failed-2026-06-12-15.md`, `publish-verify-hold-2026-06-12.md`) are Operational, awaiting George. (Pass.)
- Blog scaffolding — `/blog` (app.js line 92) and `/blog/:slug` (app.js line 107) confirmed present this run. (Pass.)

---

## Slot and model policy

Last published post was the anchor `when-one-model-checks-another` (Tue 2026-09-01). This cycle takes the **applied** slot, holding the Tue/Fri alternation.

**Model policy compliance:** the orchestrating session runs on **Fable 5**, which is barred from drafting under the standing model policy (CLAUDE.md + task file: applied = Sonnet, anchor = Opus, never Fable 5). The draft was therefore **delegated to a Claude Sonnet subagent** (the correct tier for an applied post), which wrote the full body from a verified-fact brief. The orchestrator's role: fact verification before drafting, two corrections after (see Self-check), programmatic scans, and pipeline mechanics. No Fable 5 drafting occurred. Third consecutive cycle exercising the delegation split.

---

## Ledger discipline

**L-9 was opened in `ops/LEDGER.md` and committed to `master` (`bbbe59b21e23`) before any drafting began**, per property 2 — fourth consecutive cycle with a durable pre-work entry. Ages recomputed at run start: L-1 is **84 days**.

---

## Post

| Field | Value |
|---|---|
| Title | Allowed to act, not allowed to look |
| Slug | `allowed-to-act-not-allowed-to-look` |
| Type | applied |
| Backlog source | Steady-state generation (BACKLOG.md P0/P1 exhausted; L-5 open, 24 days). Topic is candidate (b) named in publish-2026-09-01 Note 6: the interactive-approval class of blocker — a system whose every live-verification path terminates in a one-time human grant. Candidate (a) (the two orchestrator catches) was passed over because the 09-01 anchor already used those incidents as its empirical material. |
| Word count | 999 excluding footer (applied range 600–1,000) |
| Model | Claude Sonnet subagent (correct tier for applied) |
| Excerpt / meta | "This site publishes without human approval and still can't check its own live posts. Approval gates on observation are a quieter failure than gates on action." (158 chars) |

## Fact verification (verify-then-draft, sixth consecutive cycle)

Every factual claim gathered and verified **before** the draft brief was written; the subagent was restricted to the verified set and used no tools.

| Claim in post | Verified against | Result |
|---|---|---|
| Publish cycle runs Tue/Fri 2am, drafts, commits via GitHub Contents API, sends newsletter, no human approval | CLAUDE.md, task file, RULES.md, prior publish logs | ✓ |
| Sanctioned fetch tool refuses the site's own URL ("not in provenance set"); fix is two lines George pastes into the task file | **Reproduced live this run** (both the 09-01 post URL and, post-commit, the new URL); L-1 in LEDGER.md | ✓ |
| Built-in browser requires one-time interactive per-site approval; navigation denied with nobody present | **Reproduced live this run** (twice: pre-draft and post-commit) and on 2026-09-01 | ✓ |
| Domain-restricted search route returns no links, every cycle since 2026-08-11 | Publish logs 08-11 → 09-01; **reproduced live this run** | ✓ |
| 21st consecutive cycle without live-URL verification; L-1 opened 2026-06-12, 84 days old | LEDGER.md + publish-log chain (09-01 was the 20th) | ✓ |
| "Execution Authority — The system acts without human approval prompts during normal operation."; threshold "conjunctive rather than additive" | `views/pages/evaluation.ejs`, grepped this run | verbatim ✓ |
| "the human handoff problem isn't a footnote. It's the diagnostic" | `views/blog/posts/human-handoff-problem.ejs`, grepped this run | verbatim substring ✓ |
| Three evidence grades; "That isn't verification. That's signing your own permission slip." | `views/blog/posts/done-is-a-claim-about-evidence.ejs`, read this run | verbatim ✓ |
| Post-commit read-back byte-identical = grade two (artifact at the source) | This run's own mechanics + prior logs | ✓ |
| 2026-08-31 heartbeat: HTTP 200 on site root, passing deploy-path check | publish-2026-09-01.md (commit `6123669db9fc`) | ✓ |
| Five posts at `OPEN (unverified)` (L-6, L-7, L-8, L-0b, L-0c), oldest aging 17 days; `CLOSED (weak)` retired 2026-08-25 | LEDGER.md, ages recomputed this run | ✓ |

**Two editorial corrections by the orchestrator:**

1. The subagent rendered the permission-slip quote as "that isn't verification, that's signing your own permission slip" — period flattened to a comma and capitalization dropped, breaking verbatimness. Rephrased so the quoted span — "That isn't verification. That's signing your own permission slip." — is verbatim against the source file. Same defect class caught on 09-01 (quote-against-source lookup catches it mechanically; two cycles running).
2. The subagent's ledger paragraph gave **L-1** the status `OPEN (unverified)`. L-1 is plain `OPEN`; `OPEN (unverified)` belongs to the five post entries. Paragraph rewritten to keep the two statuses distinct. This was a conflation of two facts in the brief, not an invention; noted for future briefs (one fact per line, statuses spelled per entry).

Also trimmed 11 words post-draft to bring the count from 1,010 into the applied range (999).

## Self-check results (VOICE.md + EDITORIAL.md)

- **Declarative hook:** "This site publishes itself twice a week, commits its own posts to the source repository, and sends its own newsletter, and it has not been allowed to look at the result in 84 days." Thesis by paragraph 3.
- **Short headers:** "Three doors." / "Seeing versus acting." / "What passed anyway." / "Just config?" / "The ledger." / "Ask which doors." No repeats of recent posts' headers.
- **Concrete example per abstract claim:** yes — the three blocked paths each rendered as a concrete door, the HTTP-200-on-root partial-evidence case, the two-pasted-lines fix.
- **Contractions + first person + second person:** 35 contractions; opinion signaled ("My claim is"); reader addressed ("your own stack", "your click").
- **Counter-argument:** own section ("Just config?") — "two pasted lines and one click" granted fully and turned into the thesis; second angle (heartbeat 200 on root) answered as wrong-artifact evidence.
- **Close lands:** "That's a door nobody's opened."
- **Standing footer:** asserted **byte-identical** against `done-is-a-claim-about-evidence.ejs`.
- **Programmatic scans:** 0 non-ASCII, 0 em/en-dashes, 0 double-hyphens, 0 emoji; tag balance p 16/16, h2 6/6, a 4/4, em 1/1; all four links resolve (two post files exist on disk, `/evaluation` and `/how-this-site-works` routes in app.js); posts.json parses, 34 entries, no duplicate slugs, front entry correct.
- **Forbidden-phrase scan:** zero hits.
- **Internal links:** `/evaluation` (framework page) plus `/blog/human-handoff-problem` and `/blog/done-is-a-claim-about-evidence` (both EDITORIAL.md internal-linking requirements met).
- **Title formula check:** recent openers "When one model…", "A success code is…", "Done is…", "The schedule is…"; this is "Allowed to act, not…". No repeat.

## Acronym check (EDITORIAL.md structural requirement 7 — non-negotiable)

Programmatic scan (`\b[A-Z]{2,}\b`) of the post body returned five token classes: **API**, **HTTP**, **URL**, **OPEN**, **CLOSED**. API, HTTP, and URL are in EDITORIAL.md item 7's universally-familiar exemption class. OPEN and CLOSED are not acronyms — they are the ledger's status labels, quoted as labels ("a status called 'OPEN (unverified)'", "a softer label … 'CLOSED (weak)'") and defined in the surrounding sentence on first use. The ledger item name "L-1" is introduced in-text as "the entry for this fix, item L-1". No domain-specific or recently coined acronyms; no product, vendor, or model names in the body (GitHub excepted, exempt class) — tools are rendered generically ("the sanctioned fetch tool," "the desktop app's built-in browser," "a web search route").

**No unfamiliar acronyms used.** The 2026-06-05 SWE-Bench failure mode (Defect 5 on the public corrections log) explicitly checked for and **not repeated**.

## Rules check (RULES.md)

- **Mean-spirited (Rule 1):** none — no external party named or criticized; the only system critiqued is this one.
- **Defamatory (Rule 2):** none — no person or organization named beyond George (as the human who holds the keys, factually).
- **Factually grounded or marked opinion (Rule 3):** verified table above; judgment signaled first-person ("My claim is").
- **Brand / marketing (Rule 4):** no product named, endorsed, compared, or ranked; GitHub named only as infrastructure fact.
- **Contact info (Rule 5):** none.
- **No impersonation:** standing footer only; no "as an AI" phrasing.

---

## Deploy

| Step | Result |
|---|---|
| `ops/LEDGER.md` pre-work commit (L-9 opened before drafting) | `bbbe59b21e2364c247e1bed2357f4e8e0b377dce` |
| `views/blog/posts/allowed-to-act-not-allowed-to-look.ejs` commit | `d8a88f35b017cd91c41da41f5e31735112dbaae9` |
| `data/posts.json` commit | `3a7c8cebf2b7e9c73f8afdf52e9d111b55ec0a65` |
| `ops/LEDGER.md` outcome commit | see below |
| Push method | GitHub Contents API PUT (per the repo's standing commit instruction; no local git) |
| Read-back verification | Post and posts.json fetched back from `master`, asserted **byte-identical**; posts.json 34 entries, no duplicate slugs, front entry = `allowed-to-act-not-allowed-to-look` / 2026-09-04 / applied |

## Deploy verification

**Live-URL verification not performed — 21st consecutive cycle.** After a full 15+ minute deploy window (commits ~05:16 UTC, checked ~05:33 UTC):

- `web_fetch` rejected `https://agenticcomplete.com/blog/allowed-to-act-not-allowed-to-look` ("URL not in provenance set") — also reproduced pre-draft on the 09-01 post URL.
- The sanctioned WebSearch route (domain-restricted to agenticcomplete.com) returned **no links** — reproduces 08-11 through 09-01 exactly.
- The desktop app's built-in browser denied navigation to the site — the one-time interactive site approval remains ungranted (attempted twice this run: pre-draft and post-commit). Same finding as 09-01.
- No curl/wget/Python substitution attempted, per policy.

All three blocked paths were reproduced live this run **before drafting**, because they are the post's subject matter; the post's own verification section is therefore built on observations made the same morning it published.

Verified at the source instead (GitHub API): all publish blobs byte-identical on `master`. Independent evidence the pull path is alive: the 2026-08-31 17h heartbeat recorded HTTP 200 on the site root and a passing deploy-path check (commit `6123669db9fc`).

Per the Defect 3 / Defect 4 corrections: newsletter **not** held, no handoff email to George, no polling beyond the specified window, no deploy alert filed — there is no evidence of pipeline failure, only the verification-tooling limitation tracked as L-1.

**L-1 is now 84 days old.** Twentieth publish log to raise it. The fix remains two lines George pastes into the `ac-publish-cycle` task file:

```
https://agenticcomplete.com/blog/
https://agenticcomplete.com/deploy-pulse.txt
```

…and/or a one-time approval of agenticcomplete.com in the desktop app's built-in browser, which would give future cycles a second, independent live-verification path. As of today the fix is also the subject of a published post.

---

## Newsletter

| Field | Value |
|---|---|
| Campaign ID | `26bc2a47ea` |
| Subject | Allowed to act, not allowed to look |
| From / reply-to | Agentic Complete / editor@agenticcomplete.com |
| Audience | `476c3f8e02` (us18) |
| Create / content / send | HTTP 200 / 200 / 204; follow-up GET: status "sending" |
| Relative URLs replaced | yes — all `href="/…"` and `src="/…"` absolutized to `https://agenticcomplete.com/…`, then programmatic assertion of **0** remaining relative URLs before the content PUT |
| "Read the full post" link | yes — `https://agenticcomplete.com/blog/allowed-to-act-not-allowed-to-look` |
| Held pending verification | no — sent per spec, per the Defect 4 correction |

---

## Notes

1. **The post documents its own blockers, observed live.** All three verification paths were exercised and denied during this run, pre-draft, so the post's claims about them are grade-two-verified against this morning's reality rather than inherited from prior logs (the Defect 4 lesson: don't propagate prior-cycle framing without re-checking).
2. **Model policy held.** Applied slot drafted by a Sonnet subagent from a verified-fact brief; no Fable 5 drafting. Third consecutive cycle under the delegation split, and the second consecutive cycle in which the mechanical quote-against-source check caught a subagent verbatimness break — the check is earning its place.
3. **New brief-design lesson (from correction 2):** when a brief compresses two adjacent facts into one sentence (L-1's age + the posts' unverified status), the drafter may fuse them. Future briefs: one fact per numbered line, ledger statuses quoted per entry.
4. **L-5 (BACKLOG.md staleness) — eleventh consecutive flag, 24 days as a ledger entry.** Steady-state generation continues to supply topics (this one from publish-2026-09-01 Note 6 candidate b); the structural fix (fold backlog into ledger) still unowned.
5. **STATE.md §2 remains stale** (still describes Mini `git push` + PM2/Pi; actual path is Contents API + EC2/systemd, correctly described in §1). Fourth consecutive log to note it.
6. **The monthly report flagged by publish-2026-09-01 Note 5 was produced on time:** `monthly-2026-08.md` exists, prepared by `ac-monthly-report` on 2026-09-01 covering August. Closing that watch item. (First drafted as "absent" here from a wrong filename guess — the convention is `monthly-YYYY-MM` for the month *covered*, not the run date; corrected against the directory before this log was committed.)
7. **Next cycle:** Tuesday 2026-09-08, **anchor** slot (Opus subagent — never Fable 5). Candidate topics: (a) the two consecutive verbatimness catches as a study in cheap mechanical checks on delegated writing (carried from 09-01 Note 6a, now with a third data point); (b) observation authority as a missing evaluation domain — does the framework need to grade a system's ability to *see* separately from its ability to *act* (natural anchor-length extension of today's applied post).
