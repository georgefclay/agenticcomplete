# Publish Cycle — 2026-09-15

**Cycle:** Tuesday run, **anchor** slot. Drafted, committed to `master`, newsletter sent. Live-URL verification blocked (**23rd consecutive cycle**); verified at the source via the GitHub API instead. Run started 05:10 UTC; last commit ~05:45 UTC.

---

## Gating checks

- `ops/reports/sign-off.md` — present, contains "Autonomous operation is authorized." (Pass.)
- `ops/alerts/` — 10 files, **zero IMMEDIATE-level**. Eight carried the 2026-08-25 RETIRED banner at run start; the remaining two (`linkedin-post-failed-2026-06-12-15.md`, `publish-verify-hold-2026-06-12.md`) were Operational, 95 days old, non-recurring, and were annotated RETIRED by this run (housekeeping item carried since the 2026-09-03 heartbeat). All ten now retired. (Pass.)
- Blog scaffolding — `/blog` (app.js line 92) and `/blog/:slug` (app.js line 107) confirmed present. (Pass.)

---

## Prior-run cleanup (the 09-08 and 09-11 dead runs)

- **L-10 outcome recorded** in `ops/LEDGER.md` (the 09-11 run committed its post and then died before the newsletter, the report, and the ledger close). Now `OPEN (unverified)`: post commits `7f2a53d3a273` … `378cbc8bf577` on 09-11 at 05:23 UTC; **no newsletter was ever created** (Mailchimp's most recent campaign before this run was `26bc2a47ea`, 09-04); no `publish-2026-09-11.md`.
- **`publish-2026-09-11.md` not written retroactively.** The commits are on record; the model used, the self-check results, and the run's own reasoning are not reconstructible from them, and a log written four days later by a different run would be a fabrication of that evidence. The absence stands and is noted here.
- **Orphaned newsletter for `the-run-died-the-entry-didnt`:** the task file has no instruction for mailing a post a previous run committed but never sent, and the Defect 4 correction bars this run from inventing one. Not sent as its own campaign. Today's campaign carries a one-line "You may have missed" link to it, which is a content choice inside the send the task file does specify. If George wants a standalone issue for the 09-11 post, that is a by-hand send or a one-line prompt edit.

---

## Slot and model policy

Last published post was the anchor `the-run-died-the-entry-didnt` (Fri 2026-09-11, taken in the Friday slot because the Tuesday run died). Today is Tuesday, so this cycle takes the **anchor** slot on the calendar. Two anchors in a row is the consequence of last week's substitution, not a policy change; Friday 2026-09-18 is the applied slot.

**Model policy compliance:** the orchestrating session runs on **Fable 5.1**, which is barred from drafting under the standing model policy (CLAUDE.md + task file: anchor = Opus 4.8, never Fable 5). The draft was **delegated to a Claude Opus subagent** (the Agent tool's `opus` tier, the correct tier for an anchor), which wrote the full body from a verified-fact brief using Read and Write only (no web tools, no questions). The orchestrator's role: topic selection, fact verification before drafting, seven post-draft corrections (see Self-check), programmatic scans, and pipeline mechanics. No Fable 5 drafting occurred. Subagent cost: ~112K tokens, 12 tool uses, ~9.5 minutes.

---

## Ledger discipline

**L-12 was opened in `ops/LEDGER.md` and committed to `master` (`44aec49342c8`) before any drafting began**, per property 2; the same commit recorded L-10's outcome and recomputed all ages. L-12's outcome was written after the newsletter send and committed with this log. Ages at run start: **L-1 95 days**, L-5 35, L-3 28, L-4 25, L-0b 28, L-0c 25, L-6 21, L-7 18, L-8 14, L-9 11, L-10 7.

---

## Post

| Field | Value |
|---|---|
| Title | Cron is the wrong shape for an agent |
| Slug | `cron-is-the-wrong-shape` |
| Type | anchor |
| Backlog source | Steady-state generation (BACKLOG.md P0/P1 exhausted; L-5 open, 35 days). Topic is the open design question left in CORRECTIONS.md Defect 6, remediation item 2 ("Recorded as a design question, not decided here"): the post decides it. It is the natural successor to the 08-18 / 09-11 anchors and turns the site's failure record into a framework claim (trigger shape vs. control loop) rather than a fourth dead-run narrative; the September incidents occupy roughly a third of the body, the general argument and two external examples the rest. Candidates passed over: the two verbatimness catches as a study in cheap mechanical checks (carried from 09-01/09-04; still available for an applied slot). |
| Word count | 2,407 excluding footer (anchor range 1,500–2,500) |
| Model | Claude Opus subagent (correct tier for anchor) |
| Excerpt / meta | "A schedule-triggered agent responds at one speed. A reconciliation loop lets the gap between owed and done decide, so noticing and fixing become one act." (153 chars) |

## Fact verification (verify-then-draft, seventh consecutive cycle)

Every factual claim was gathered and verified **before** the draft brief was written; the subagent was restricted to the verified set (18 numbered facts) and used no tools beyond Read/Write.

| Claim in post | Verified against | Result |
|---|---|---|
| Six scheduled jobs; publish Tue/Fri early UTC; heartbeat twice daily; LinkedIn Tue/Fri mid-morning | CLAUDE.md, STATE.md §4, report filenames | ✓ |
| Three publish runs fired with no post since 2026-04-28: 07-07, 08-14, 09-08 | CORRECTIONS.md Defect 6; posts.json (no 07-07 or 08-14 entry) | ✓ |
| 09-08 run: fired 05:10 UTC, one commit at 05:11:17 UTC, nothing else | GitHub commit log (`2a50ed4328be`) read this run | ✓ |
| 09-11 run: post committed 05:23 UTC in four commits, then no newsletter / report / ledger close | Commit log (`7f2a53d`, `03ceb1c`, `d99775d`, `378cbc8`); Mailchimp campaigns list read this run (last = `26bc2a47ea`); `ops/reports/` listing | ✓ |
| Eight heartbeats between the dead run and end of 09-11, each "Anomalies: none in heartbeat scope"; heartbeat checks 200 / sign-off / alerts / push, does not read the ledger | Eight report files `heartbeat-2026-09-08-05` … `heartbeat-2026-09-11-17`, grepped this run (20 of 20 September heartbeats carry the line) | ✓ |
| LinkedIn job noticed both, ~15:10 UTC, no publish authority, email only for LinkedIn failures, no email to George | weekly-2026-09-14.md; Defect 6 | ✓ |
| Repair 72 h after the 09-08 dead run, 62 h after detection | Defect 6 (verbatim figures) | ✓ |
| Dead-run repair costs 72, 96, 72 hours (07-07→07-10, 08-14→08-18, 09-08→09-11) | Calendar arithmetic on the three dates (Tue→Fri = 72 h, Fri→Tue = 96 h) | ✓ (orchestrator correction; subagent had written "twice … 72") |
| Ledger built 2026-08-21; every run since 08-25 opened its entry before drafting; today's run did (L-12) | LEDGER.md; this run's own commit `44aec49342c8` | ✓ |
| L-3 opened 08-18 (28 days), L-4 opened 08-21 (25 days) | LEDGER.md, ages recomputed this run | ✓ |
| Job prompts edited by George with a scheduled-task tool; system doesn't edit its own | CLAUDE.md ("edit them with the scheduled-task update tool") | ✓ |
| $50/month token budget | BUDGET.md | ✓ |
| "104 times a year" (publish job entries per year) | 2 × 52 | ✓ |
| Evaluation page quotes (Adaptive Response, Feedback Interpretation, "conjunctive rather than additive") | `views/pages/evaluation.ejs`, substring-asserted this run | verbatim ✓ |
| Architecture page quotes (three components, diagram order, "loop until completion", "Closed-loop continuity…") | `views/pages/architecture.ejs`, substring-asserted | verbatim ✓ |
| Maturity page quotes (Level 1 definition; "without human handoffs") | `views/pages/maturity.ejs`, substring-asserted | verbatim ✓ |
| Defect 6 item 2 quote | `ops/CORRECTIONS.md`, substring-asserted (the public `/corrections` entry does not carry the remediation text, so the post links both the page and the repo file) | verbatim ✓ |
| Kubernetes controllers doc: "In robotics and automation, a control loop…" and the thermostat sentences | https://kubernetes.io/docs/concepts/architecture/controller/ fetched this run via WebSearch→web_fetch; the page body is client-rendered and the fetch returned navigation plus the page's meta description, which carries both quoted passages verbatim. The controller-behaviour sentence is paraphrased, not quoted, for that reason. | verbatim ✓ (quotes); paraphrase (behaviour) |
| GNU make manual: "The make program uses the makefile data base and the last-modification times…" and "You can use it to describe any task…" | https://ftp.gnu.org/old-gnu/Manuals/make-3.79.1/html_chapter/make_1.html fetched this run, full text | verbatim ✓ |

**Seven orchestrator corrections to the Opus draft** (all before commit):

1. "runs every large computing cluster and every software build on earth" → "runs container clusters and software builds" (unsourced universal claim, Rule 3).
2. "That is the architecture of most autonomous agents shipping right now" → prefixed "My read is that…" (industry claim stated as fact → marked opinion, Rule 3).
3. "twelve tiny runs a day is about six heartbeats' worth of work" → "twelve heartbeats' worth" (arithmetic error).
4. "twice this year it cost 72" → "the three dead runs this year cost 72, 96, and 72" (August's miss was Fri→Tue = 96 h; three misses, not two).
5. "Here's the part that stings" → "Here's the awkward part" (verbatim opening move from the 09-11 anchor, which the brief barred).
6. "The second run costs nothing." → "costs almost nothing."
7. `/corrections` link for the Defect 6 quote → links both the public page and the repo's `ops/CORRECTIONS.md`, because the quoted remediation text is only in the latter.
Plus the GNU expansion (see Acronym check). Same brief-design lesson as 09-04: the drafter compresses adjacent numbers ("twice", "six") and the orchestrator's arithmetic pass catches them; the pass earned its place again.

## Self-check results (VOICE.md + EDITORIAL.md)

- **Declarative hook:** "Cron is the wrong shape for an agent." Thesis stated fully by paragraph 3.
- **Short headers:** "What decides?" / "The thermostat." / "Make has no clock." / "September, in numbers." / "The diagram was already right." / "Just a faster cron?" / "My proposal." / "What would yours do?" No repeats of the 08-18 or 09-11 anchors' headers.
- **Concrete example per abstract claim:** yes — thermostat, Kubernetes replica count, `make` up-to-date check, the eight clean heartbeats, the 144-posts-a-day hypothetical, the 72/96/72 hours.
- **In-line arithmetic:** the latency and cost paragraph (12 runs vs. 2 heartbeats; 72–96 h vs. <2 h; ~40×).
- **Contractions + first person + second person:** throughout ("I'm deciding it", "you can run it this afternoon").
- **Opinion signaled:** "My read is" (×3), "My proposal is", "I think", "I don't think it's close."
- **Counter-argument:** own section ("Just a faster cron?") with the frequency objection and the budget objection both granted and answered.
- **Close lands:** "The thermostat on your wall does the harder job, and it has never once needed to know what time it is."
- **Standing footer:** asserted **byte-identical** against `the-run-died-the-entry-didnt.ejs`.
- **Programmatic scans:** 0 non-ASCII, 0 em/en-dashes, 0 double-hyphens, 0 emoji; tag balance p 32/32, h2 8/8, a 10/10, em 1/1, code 2/2; all internal links resolve (`/architecture`, `/evaluation`, `/maturity`, `/corrections`, `/how-this-site-works` routes; two post files exist on disk); posts.json parses, 36 entries, no duplicate slugs, front entry correct.
- **Forbidden-phrase scan:** zero hits.
- **Internal links:** three framework pages plus `/corrections`, and `/blog/schedule-is-not-a-work-queue`, `/blog/the-run-died-the-entry-didnt`. External: two primary sources (Kubernetes docs, GNU make manual) plus the repo file.
- **Title formula check:** recent openers "The run died…", "Allowed to act…", "When one model…", "A success code…", "Done is…", "The schedule is…"; this is "Cron is the wrong shape…". No repeat.

## Acronym check (EDITORIAL.md structural requirement 7 — non-negotiable)

Programmatic scan (`\b[A-Z]{2,}\b`) of the post body returned three token classes: **UTC**, **HTTP**, **GNU**.

- **UTC** — expanded on first mention: "early morning Coordinated Universal Time (UTC)". Subsequent mentions short form. ✓
- **HTTP** — EDITORIAL.md exempt class. ✓
- **GNU** — the subagent's draft used "GNU make" as a bare product name. Rewritten by the orchestrator: `make` is introduced first in plain English ("make, the build tool that turns source files into finished programs"), and GNU is glossed on first mention in a parenthetical ("GNU is the free software project that maintains it; the name is a recursive acronym for "GNU's Not Unix""). ✓
- **cron** — a word, not an acronym; introduced on first use as "the Unix job scheduler: you hand it a time and a command, and at that time it runs that command." ✓
- **Kubernetes** — product name, introduced as "the open source system that runs containerized applications across fleets of machines." ✓
- **L-3, L-4, L-12** — ledger item labels, each introduced in-text with what the item is. `ac-publish-cycle` appears only inside the verbatim Defect 6 quote, preceded by "the instructions each scheduled job runs under." ✓

**No unexpanded domain-specific acronyms.** The 2026-06-05 SWE-Bench failure mode (public corrections log, Defect 5) explicitly checked for and not repeated.

## Rules check (RULES.md)

- **Mean-spirited (Rule 1):** none. No external party criticized; the only system critiqued is this one.
- **Defamatory (Rule 2):** none. Kubernetes and GNU make are described from their own documentation; George is named only as the person who edits job prompts (factual).
- **Factually grounded or marked opinion (Rule 3):** verified table above; two draft sentences that stated opinion as fact were rewritten (corrections 1–2).
- **Brand / marketing (Rule 4):** Kubernetes and make are examples of a pattern, not recommendations; no superlatives, no "use X for Y".
- **Contact info (Rule 5):** none in the post; the newsletter footer names `editor@agenticcomplete.com` only.
- **No impersonation:** standing footer only; no "as an AI" phrasing.

---

## Deploy

| Step | Result |
|---|---|
| `ops/LEDGER.md` pre-work commit (L-12 opened, L-10 outcome recorded, before drafting) | `44aec49342c88ef29d77aeb3d18cce327b9daa20` |
| `views/blog/posts/cron-is-the-wrong-shape.ejs` commit | `00feb59f81c519c1f676fdf63012b7599dac107d` |
| `data/posts.json` commit | `0c54fc4351d487b53a80354897b4df4e863c7790` |
| `ops/LEDGER.md` outcome commit + this log | see the commits following this file's |
| Push method | GitHub Contents API PUT (per the repo's standing commit instruction; no local git) |
| Read-back verification | Post and posts.json fetched back from `master`, asserted **byte-identical**; posts.json 36 entries, no duplicate slugs, front entry = `cron-is-the-wrong-shape` / 2026-09-15 / anchor |

## Deploy verification

**Live-URL verification not performed — 23rd consecutive cycle.** After a 16-minute deploy window (commits ~05:22 UTC, checked 05:38 UTC):

- `web_fetch` rejected `https://agenticcomplete.com/blog/cron-is-the-wrong-shape` ("URL not in provenance set").
- The sanctioned WebSearch route (domain-restricted to agenticcomplete.com) returned **no links**.
- The desktop app's built-in browser refused the site — the one-time interactive site approval remains ungranted.
- No curl/wget/Python substitution attempted, per policy.

Verified at the source instead (GitHub API): all publish blobs byte-identical on `master`. Independent evidence the pull path is alive: the 2026-09-14 17h heartbeat recorded HTTP 200 on the site root, the deploy-pulse sentinel served, and a passing deploy-path push (commit `c6aea22652d9`).

Per the Defect 3 / Defect 4 corrections: newsletter **not** held, no handoff email to George, no polling beyond the specified window, no deploy alert filed — there is no evidence of pipeline failure, only the verification-tooling limitation tracked as L-1.

**L-1 is now 95 days old.** The fix remains two lines George pastes into the `ac-publish-cycle` task file:

```
https://agenticcomplete.com/blog/
https://agenticcomplete.com/deploy-pulse.txt
```

…and/or a one-time approval of agenticcomplete.com in the desktop app's built-in browser (scope "site").

---

## Newsletter

| Field | Value |
|---|---|
| Campaign ID | `3d92dfba20` |
| Subject | Cron is the wrong shape for an agent |
| From / reply-to | Agentic Complete / editor@agenticcomplete.com |
| Audience | `476c3f8e02` (us18) |
| Create / content / send | HTTP 200 / 200 / 204; follow-up GET: status "sending" |
| Relative URLs replaced | yes — all `href="/…"` and `src="/…"` absolutized to `https://agenticcomplete.com/…`, then programmatic assertion of **0** remaining relative URLs before the content PUT |
| "Read the full post" link | yes — `https://agenticcomplete.com/blog/cron-is-the-wrong-shape` |
| Extra | one-line "You may have missed" link to `/blog/the-run-died-the-entry-didnt` (published 09-11 without a newsletter) |
| Held pending verification | no — sent per spec, per the Defect 4 correction |

---

## Notes

1. **The post decides Defect 6's open design question** in favour of a single reconciler job (read `LEDGER.md` as desired state, read repo + live site as actual state, exit early or do the oldest owed thing). That is a proposal only; the change is a scheduled-task prompt edit that George makes. Ledger items L-3 and L-4 are the operational form of the same request; the post argues L-4 is the one that matters.
2. **Model policy held.** Anchor drafted by an Opus subagent from a verified-fact brief; no Fable 5 drafting. Fourth consecutive cycle under the delegation split; the orchestrator's arithmetic and verbatim passes caught seven items, two of them factual (corrections 3–4).
3. **Housekeeping closed:** both remaining June alerts annotated RETIRED (local only; `ops/alerts/` is gitignored). `ops/alerts/` now has zero open files.
4. **`publish-2026-09-11.md` deliberately not reconstructed** (see Prior-run cleanup). The weekly report's "model policy cannot be confirmed for 09-11" stands.
5. **L-5 (BACKLOG.md staleness), 35 days.** Steady-state generation continues to supply topics; structural fix still unowned.
6. **STATE.md remains stale** on §2 (still describes Mini `git push` + PM2/Pi; actual path is Contents API + EC2/systemd, correct in §1), on LinkedIn follower count (7 vs. 9), "Last updated 2026-08-01". Sixth consecutive log to note it. Not edited this run: STATE.md is a policy-adjacent file and the task file doesn't ask this cycle to maintain it; flagging for a dedicated pass.
7. **posts.json encoding:** this run rewrote the file with literal UTF-8 characters rather than `\uXXXX` escapes (four older excerpts contain dashes). JSON-equivalent; Node parses both; noted so the next diff isn't misread as content change.
8. **Next cycle:** Friday 2026-09-18, **applied** slot (Sonnet subagent — never Fable 5). Candidate topics: (a) the mechanical-checks study (two cycles' worth of orchestrator catches on delegated drafts: verbatimness ×2, arithmetic ×2, opinion-as-fact ×2) as a field note on cheap verification of model-written text; (b) "what an exit-early run actually reads" — a concrete spec for the reconciler's two-file check, if George has acted on today's proposal by then; (c) the 09-11 partial-run variant (artifacts committed, downstream steps skipped) as its own failure class distinct from the dead run.
