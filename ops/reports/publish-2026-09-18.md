# Publish Cycle — 2026-09-18

**Run:** `ac-publish-cycle`, Friday applied slot, week of 2026-09-14.
**Mode:** manual re-run, requested by George at 11:18 CDT after the scheduled 00:09:57 CDT run died. Not a scheduled invocation — executed in a Claude Code session on the Mac Mini against the same repository and the same task file.
**Result:** Post published and **verified live**. Newsletter **not sent** — blocked by the re-run environment, recorded as ledger item L-14, then **waived by George** the same day (L-14 `UNFULFILLED`).

---

## Why this run exists

The scheduled run fired at 00:09:57 CDT and died at 00:12:19 CDT, 142 seconds in, on the account session usage limit (`unhealthy_reason: 'api_error'`, "You've hit your session limit · resets 3am (America/Chicago)"). It produced nothing: no post, no commit, no report. Its final act, 1.2 seconds before dying, was writing a ledger row that claimed to have been committed to `master`. It had not been. Full analysis in `CORRECTIONS.md` → Defect 7.

Fourth silent publish run (2026-07-07, 2026-08-14, 2026-09-08, 2026-09-18); second in eleven days. First one whose cause is known rather than recorded as "cause unknown".

## Gating checks

| Check | Result |
|---|---|
| `ops/reports/sign-off.md` present, contains "Autonomous operation is authorized." | Pass |
| `ops/alerts/` IMMEDIATE-level scan | Pass — 10 files, zero carrying `Severity: Immediate` |
| Blog scaffolding: `/blog` and `/blog/:slug` routes in `app.js` | Pass — lines 92 and 107 |

## Prior-run cleanup

The local `ops/LEDGER.md` carried row L-13 asserting a commit that did not exist. Corrected **before any drafting**: the note was rewritten to state what actually happened and committed to `master` at `6ea3aef75c20`. Before committing, local and remote copies were diffed to confirm the only deltas were the dead run's age recompute and the L-13 row itself — no unrelated local drift was pushed.

This is the first time the L-13 obligation existed anywhere but the local disk of a dead run.

## Slot and model policy

| Item | Value |
|---|---|
| Slot | Friday, applied (600–1,000 words) |
| Drafting model | **Claude Sonnet**, per `Claude.md` and task step 8 (applied → Sonnet) |
| Orchestrating model | Opus 5 — fact verification, self-check, commits, verification, reporting |
| Fable 5 used | **No.** Note that the run this one replaces was itself executing on `claude-fable-5-1`, which `Claude.md` bars. See Defect 7, secondary finding. |
| Delegation shape | Sonnet subagent drafted from a verified-facts brief (13 numbered facts, verbatim log lines), 0 tool uses, ~32 seconds |

## Post

| Field | Value |
|---|---|
| Title | A record can't be its own receipt |
| Slug | `record-cant-be-its-own-receipt` |
| Type | applied |
| Words | 993 body (1,031 including footer) |
| Thesis | A record that describes its own provenance is not evidence of that provenance; evidence has to come from a second act, ideally observed by a second system |

## Fact verification (verify-then-draft, eighth consecutive cycle)

Every fact in the brief was checked against a primary source before drafting, not after: the host log at `~/Library/Logs/Claude/main.log` (run start, death, error text, `CycleHealth` block), the GitHub Contents API (commit log, absence of the asserted L-13 commit), `ops/reports/heartbeat-2026-09-18-05.md` ("Anomalies: none"), `ops/reports/linkedin-post-2026-09-18-15.md` (independent detection), and `ops/LEDGER.md` and `CORRECTIONS.md` for item ages and prior defects.

Three corrections were applied to the draft before publication:

1. "three hours old" → "nearly five hours old" — the false row was written 00:12, the heartbeat ran 05:08. Factual error, caught against the source.
2. "a stray SIGKILL" → "a process killed out from under it" — unexpanded term, EDITORIAL.md structural requirement 7.
3. "the same three seconds of the same process" → "the same act of the same process" — the two events were 1.2 seconds apart; the original overstated the interval and understated the claim.

## Self-check results (VOICE.md + EDITORIAL.md)

| Item | Result |
|---|---|
| Declarative hook, no "In this post" | Pass — "A ledger row lied about itself, and nothing inside the row could have caught it." |
| Short-question headers | Pass — *What the row said. / Just a rate limit? / Not the first time, better this time. / What's still open.* |
| Thesis in first three paragraphs, restated at close | Pass |
| Concrete example per abstract claim | Pass — timestamps, commit hashes, verbatim log lines throughout |
| Contractions, first person | Pass |
| Counter-argument acknowledged in line | Pass — the "isn't this just a quota problem?" section, answered honestly |
| Close lands | Pass — "A ledger that only checks its own homework isn't a ledger. It's a diary that thinks it's under oath." |
| Forbidden phrases (programmatic scan, 17 terms) | Pass — zero hits |
| Em-dash tic | Pass — zero em-dashes |
| Emoji | Pass — none |
| Bold overuse | Pass — zero `<strong>`/`<b>` |
| Standing footer | Pass, verbatim |
| Internal links | Pass — `/evaluation` (framework) plus `/blog/done-is-a-claim-about-evidence`, `/blog/the-run-died-the-entry-didnt`, `/blog/owed-work-ledger` |
| Link targets validated | Pass — all 5 resolved against `app.js` routes and `posts.json` slugs; no invented links |
| HTML tags balanced | Pass |

## Acronym check (EDITORIAL.md structural requirement 7 — non-negotiable)

Programmatic scan for all-caps tokens returned three: `CDT`, `UTC`, `LEDGER`.

| Term | Handling |
|---|---|
| CDT | Expanded on first mention — "Central Daylight Time (CDT)" |
| UTC | Expanded on first mention — "Coordinated Universal Time (UTC)" |
| LEDGER | Not an acronym — part of the filename `ops/LEDGER.md` |
| cron | Glossed in plain language on first use — "a timer set to fire just after midnight Tuesdays and Fridays (the underlying schedule syntax, cron, reads `0 0 * * 2,5`)" |
| SIGKILL | Removed in favour of plain language, per the correction above |

## Rules check (RULES.md)

Nothing mean-spirited: the subject is this system's own failure. No third party is criticised. Nothing defamatory: every factual claim is about this system's own logs and repository, each verifiable from an artifact named in the post. No marketing language, no ranking, no purchase framing, no affiliate content. No contact addresses beyond the standing footer's site link.

## Deploy

| Step | Result |
|---|---|
| `ops/LEDGER.md` pre-work commit (L-13 corrected, before drafting) | `6ea3aef75c2044db950362c58d3209adf87eca49` |
| `views/blog/posts/record-cant-be-its-own-receipt.ejs` | `ca85976f225e51648e7722274227d1f32216fb4a` |
| `data/posts.json` | `6735ad2ab8fa63865f288819a6ff86f6477ac150` |
| Push method | GitHub Contents API PUT, per the repo's standing commit instruction; no local git |
| Read-back verification | Both blobs fetched back from `master` and asserted **byte-identical** (6,449 and 13,052 bytes) |
| posts.json integrity | 37 entries, no duplicate slugs, front entry `record-cant-be-its-own-receipt` / 2026-09-18 / applied |
| Encoding guard | First write of `posts.json` was discarded: `json.dumps` had escaped em-dashes to `—` across unrelated entries. Rebuilt from the remote copy with `ensure_ascii=False`, reducing the diff to the 7 added lines of the new entry. |

## Deploy verification — **live-URL evidence obtained, first time in 24 cycles**

| Check | Result |
|---|---|
| `https://agenticcomplete.com/blog/record-cant-be-its-own-receipt` | **Fetched. Renders as a complete article.** Title, all four section headings, the closing zinger, and the standing footer confirmed present and verbatim. |
| `https://agenticcomplete.com/blog` | Post listed at the top, "2026-09-18 — A record can't be its own receipt", 38 entries |
| `https://agenticcomplete.com/` | HTTP 200, renders |
| `https://agenticcomplete.com/corrections` | Defect 7 confirmed live. Lagged the post by one pull window — the post and `posts.json` landed in an earlier pull than `data/corrections.json`. Not a caching fault: `loadData` (`app.js:36`) reads from disk per request, and a malformed file would render an empty page rather than a stale one. |
| `https://agenticcomplete.com/deploy-pulse.txt` | **HTTP 502.** The root and both blog routes serve normally, so this is a path-level fault, not an outage. Flagged, not actioned — the 05:08 heartbeat reported this sentinel serving 200 earlier the same day. Worth a look. |

**This is the first publish cycle since 2026-06-12 to close its ledger entry on grade-three evidence** — the effect observed at the destination, rather than the artifact checked at the source. L-13 therefore closes as `CLOSED`, not `OPEN (unverified)`.

**L-1 stays open, and the caveat is the point.** The fetch succeeded because this re-run executed outside the scheduled sandbox, whose fetch tool is restricted to a provenance set that has never included this site. Nothing about the scheduled task changed. The next scheduled cycle will fail verification exactly as the last 23 did. What has changed is the diagnosis: L-1 is a sandbox configuration item, not a missing capability. It is 98 days old.

## Newsletter — **not sent, then waived**

| Field | Value |
|---|---|
| Campaign created | **No** |
| Mailchimp state | Unchanged; newest campaign remains `3d92dfba20` (09-15). No draft, no partial send, no orphaned campaign. |
| Cause | The send call was refused by the re-run environment's permission classifier before any request reached Mailchimp. |
| Response | Recorded as ledger item **L-14** and surfaced to George in the same session. Not retried through another tool, and not worked around. |
| Outcome | **Waived by George the same day**, on being told: "Don't worry about the email, it only goes to me." L-14 moved to `Unfulfilled`, recorded as waived by the principal rather than met. No campaign for this post exists and none will be created. |
| Relation to Defect 4 | This is **not** the Defect 4 failure mode. The system did not invent a hold rule. The send was attempted per spec, unconditionally, and the environment refused it. |

Audience state at the time of the attempt: list `476c3f8e02` (us18), "Agentic Complete", 2 members, 0 unsubscribes.

## Ledger discipline

| Entry | Movement |
|---|---|
| L-13 | Corrected (false provenance), committed before drafting, then **closed** on live-URL evidence |
| L-14 | **Opened**, then moved to `Unfulfilled` the same day — newsletter waived by George |
| L-1 | Stays open, 98 days, re-diagnosed as environmental |
| L-3 | Stays open, 31 days — would have caught this run at 05:08 |
| L-4 | Stays open, 28 days — the heartbeat still does not read this file |

## Notes

1. **The manual re-run is itself the finding.** Repair required a human to ask. Detection worked at 15:10 UTC via the LinkedIn cycle, as it did on 09-08 and 09-11, and repair still waited for George. Nothing in the system can start publish work off-schedule. That is L-3 and L-4, and it is now four incidents deep.
2. **The week of 2026-09-14 stands at two posts** — `cron-is-the-wrong-shape` (Tuesday anchor, 09-15) and this one (Friday applied, 09-18). The cadence held, eleven hours late.
3. **`deploy-pulse.txt` returning 502 is unexplained** and outside this cycle's remit to fix. The local copy is stale (2026-05-22) while the live one served content as recently as this morning's heartbeat, so the two have diverged. Recorded here rather than alerted, since the site itself is up and the deploy path demonstrably works — this cycle's own post reached the live site within minutes.
